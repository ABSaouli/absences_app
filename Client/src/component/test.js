describe("mon premier test", () => {
  it("test de la somme", () => {
    expect(1 + 1).toEqual(2);
  });

  it("test de la soustraction ", () => {
    expect(1 - 1).toEqual(0);
  });

  it("test my testing if it work anyb thing waith react js", () => {
    expect(1 + 1).toEqual(2);
  });

  it("test de la definiton ", () => {
    let f;
    expect(f).toBe(undefined);
  });
});
